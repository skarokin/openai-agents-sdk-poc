calculator:
  import_path: agent.tools.builtin:calculator
  enabled: true
  allowed_roles: [calculator]

approval_demo:
  import_path: agent.tools.builtin:approval_demo
  enabled: true
  allowed_roles: [approval_user]
  hitl: true

protected_subagent:
  import_path: agent.tools.subagents:protected_subagent
  enabled: true
  allowed_roles: [subagent_user]
  hitl: true

open_subagent:
  import_path: agent.tools.subagents:open_subagent
  enabled: true
  allowed_roles: [subagent_user]

authentication_demo:
  import_path: agent.tools.builtin:authentication_demo
  enabled: true
  allowed_roles: [auth_user]

auth_approval_demo:
  import_path: agent.tools.builtin:auth_approval_demo
  enabled: true
  allowed_roles: [auth_user]
  hitl: true

notes_mcp:
  type: mcp
  import_path: agent.tools.notes_mcp:create_notes_mcp
  enabled: true
  allowed_roles: [mcp_user]
  tools:
    list_notes:
      allowed_roles: [mcp_user]
    delete_note:
      allowed_roles: [mcp_user]
      hitl: true

[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "skarokin", email = "sean.kim0327@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai-agents>=0.22.0",
]

[project.scripts]
agent = "agent:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
